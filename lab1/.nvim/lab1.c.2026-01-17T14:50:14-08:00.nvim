// #define_GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {

  char *line = NULL;
  size_t size = 0;
  size_t read;

  // Loops forever
  while (read = getline(&line, &size, stdin) != -1) {
 
    printf("Please enter some text: ");
  
    printf("%s\n", line);

  }

  free(line);

  return 0;
}
