// #define_GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
  
  printf("Please enter some text: ");

  char *line = NULL;
  size_t len = 0;

  read = getline(&line, &len, stdin);
  printf("%s\n", line);

  free(line);



  return 0;
}
