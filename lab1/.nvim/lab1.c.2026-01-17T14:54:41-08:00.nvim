// #define_GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {

  printf("Please enter some text: ");

  char *line = NULL;
  size_t size = 0;
  ssize_t read;

  read = getline(&line, &size, stdin);
  printf("%s", read);

  free(line);

  return 0;
}
